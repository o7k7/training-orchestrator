{{- if .Values.grafanaDashboard.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-dashboard
  labels:
    grafana_dashboard: "1"
data:
  dcgm-exporter.json: |
{{ .Files.Get "dashboards/dcgm-exporter.json" | indent 4 }}
{{- end }}
